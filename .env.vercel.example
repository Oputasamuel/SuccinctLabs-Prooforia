# SP1Mint Environment Variables for Vercel Deployment
# Copy these to your Vercel dashboard under Project Settings > Environment Variables

# Database Configuration (Required)
DATABASE_URL=postgresql://username:password@ep-xxx.us-east-1.aws.neon.tech/neondb?sslmode=require

# Session Security (Required)
SESSION_SECRET=your-super-secret-session-key-minimum-32-characters-long

# Email Service for Password Reset (Required for password reset feature)
SENDGRID_API_KEY=SG.your-sendgrid-api-key-here
FROM_EMAIL=noreply@yourdomain.com
FROM_NAME=SP1Mint

# Production Environment
NODE_ENV=production

# Additional Configuration (Optional)
PORT=5000
TRUST_PROXY=1